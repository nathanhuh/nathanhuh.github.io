---
layout: base.njk
title: Blog
description: Thoughts, experiences, and technical insights on software engineering, SRE, and infrastructure.
---

<section class="container py-12 md:py-16">
    <div class="max-w-6xl mx-auto">
    <header class="mb-12">
        <h1 class="text-4xl font-bold tracking-tight mb-4">Blog</h1>
        <p class="text-lg text-[#666666]">
            Thoughts, experiences, and technical insights on software engineering, SRE, and infrastructure.
        </p>
    </header>

    <div class="space-y-8">
        {% set posts = collections.blog | reverse %}
        {% if posts.length > 0 %}
            {% for post in posts %}
            <article class="border-b border-[#e5e5e5] pb-8 last:border-b-0">
                <h2 class="text-2xl font-bold tracking-tight mb-2">
                    <a href="{{ post.url }}" class="hover:text-[#666666] transition-colors duration-200">
                        {{ post.data.title }}
                    </a>
                </h2>

                <div class="flex items-center gap-4 text-sm text-[#666666] mb-4">
                    <time datetime="{{ post.data.date | date('YYYY-MM-DD') }}">
                        {{ post.data.date | date('MMMM DD, YYYY') }}
                    </time>
                    {% if post.data.tags %}
                    <div class="flex gap-2">
                        {% for tag in post.data.tags %}
                        <span class="px-2 py-1 bg-[#f5f5f5] rounded text-xs">{{ tag }}</span>
                        {% endfor %}
                    </div>
                    {% endif %}
                </div>

                {% if post.data.description %}
                <p class="text-[#666666] mb-4">{{ post.data.description }}</p>
                {% endif %}

                <a href="{{ post.url }}" class="text-sm font-medium hover:text-[#666666] transition-colors duration-200">
                    Read more →
                </a>
            </article>
            {% endfor %}
        {% else %}
            <p class="text-[#666666]">No blog posts yet. Check back soon!</p>
        {% endif %}
    </div>
    </div>
</section>
